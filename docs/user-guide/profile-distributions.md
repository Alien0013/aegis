# Profile Distributions

A profile distribution is a shareable agent shape: model/provider defaults, toolsets, skills, memory provider settings, gateway channels, and dashboard preferences. Distribution docs must separate safe config from credentials.

Use profile exports only after redaction and never include API keys, tokens, local session cookies, or private memory contents.
