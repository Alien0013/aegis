# Hooks and Event Contracts

Hooks connect external events or local lifecycle events to agent work. A hook contract should define input shape, auth, rendering, delivery target, tool permissions, retry behavior, and trace/run metadata.

Check local hook metadata with dashboard `/api/hooks` or the CLI hook commands.
