# Voice and TTS

Voice surfaces are gateway and tool capabilities. Audio input should be transcribed before entering the agent context; generated audio should be treated as an output attachment with redacted metadata.

Live voice validation requires real platform attachments or local audio fixtures, and should be recorded separately from local mocked gateway tests.
