# Browser and Web Automation

Browser and web automation are tool-gated capabilities. They should be enabled only when dependencies are present and routed through permission, redaction, and trace metadata.

Use browser tools for interactive web validation, screenshots, and local dashboard QA. Use HTTP/client tests for deterministic API validation.
