console.error('AEGIS Photon sidecar is a package surface mapped to the native relay bridge; configure gateway relay/webhook delivery until a dedicated Photon adapter is installed.');
process.exit(78);
